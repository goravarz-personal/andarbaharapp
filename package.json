{
  "name": "aadarbaharapp",
  "version": "1.0.0",
  "private": true,
  "description": "AadarBaharApp - Andar Bahar game night tracker for iOS and Android, with an API backend.",
  "workspaces": [
    "server",
    "mobile"
  ],
  "scripts": {
    "setup": "npm install --legacy-peer-deps && npm run db:setup --workspace server",
    "server": "npm run dev --workspace server",
    "mobile": "npm run start --workspace mobile",
    "db:setup": "npm run db:setup --workspace server",
    "db:seed": "npm run db:seed --workspace server",
    "test": "npm run test --workspace server",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace mobile",
    "build": "npm run build --workspace server"
  },
  "engines": {
    "node": ">=20"
  }
}
